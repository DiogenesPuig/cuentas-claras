# Supabase (front) — la anon key es pública por diseño; el acceso real lo controla RLS.
# Copiá este archivo a .env.local y completá los valores de tu proyecto.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
