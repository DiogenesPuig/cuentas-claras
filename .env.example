# Supabase (front) — la anon key es pública por diseño; el acceso real lo controla RLS.
# Copiá este archivo a .env.local y completá los valores de tu proyecto.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Microservicio de ingesta (OCR/parseo, Fase 2 — F2-1). Opcional: si falta, el OCR
# de comprobantes avisa y cae a carga manual. Ej: https://cuentas-claras-ingesta.fly.dev
VITE_INGESTA_URL=
