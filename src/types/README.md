# src/types

Tipos de dominio legibles, definidos como alias sobre los tipos generados en
`@/lib/database.types`. (Vacío hasta el ticket A2, cuando exista `database.types.ts`.)

## Contenido previsto

- `index.ts` — `Workspace`, `Member`, `Account`, `Category`, `Transaction`, `TransactionView`, etc.
