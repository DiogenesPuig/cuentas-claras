# src/features/categories/components

- `CategoryList.tsx` — lista categorías globales + propias separadas por gasto/ingreso; muestra
  alta/edición solo a owner/admin.
- `CategoryForm.tsx` — form de alta/edición de una categoría propia (name, kind, icon, color).
- `CategoryForm.test.tsx` — smoke test: nombre requerido y kind válido por defecto.
