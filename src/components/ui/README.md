# src/components/ui

Componentes generados por shadcn/ui (`npx shadcn@latest add <componente>`).
Se mantienen tal cual upstream; excluidos del lint en `.eslintrc.cjs`. (Vacío hasta que se agregue el primero.)
