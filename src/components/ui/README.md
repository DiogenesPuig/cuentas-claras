# src/components/ui

Componentes generados por shadcn/ui (`npx shadcn@latest add <componente>`).
Se mantienen tal cual upstream; excluidos del lint en `eslint.config.js`.

## Archivos

- `calendar.tsx` (MEJ-1) — calendario shadcn/ui sobre `react-day-picker` (locale español), usado por
  `DateField` (`src/components/DateField.tsx`).
